# task-0072 — TDZ переменной цикла for-of

Песочница для выполнения — [Programiz](https://www.programiz.com/javascript/online-compiler/).

## Условие
```javascript
// Разберите код: укажите точный тип ошибки и объясните, почему тело цикла не начинает выполняться.
// Исправьте только имя переменной цикла так, чтобы в консоль вывелись 1, 2 и 3.

function go(n) {
  console.log(n);

  for (const n of n.a) {
    console.log(n);
  }
}

go({ a: [1, 2, 3] });
```

<details>
<summary>Подсказка 1 — куда смотреть</summary>

Имя слева от `of` объявляет новую переменную, которая действует уже при вычислении выражения справа.

</details>

<details>
<summary>Подсказка 2 — с чего начать</summary>

Из-за TDZ правая часть не может прочитать внешний массив через уже затенённое имя.

</details>

<details>
<summary>Подсказка 3 — почти решение</summary>

Назовите элемент, например, `value`: `for (const value of n.a)` оставит справа доступ к параметру.

</details>

<details>
<summary>Решение</summary>

```javascript
function go(n) {
  console.log(n);

  for (const value of n.a) {
    console.log(value);
  }
}
```

Исходный вариант сначала выводит переданный объект, затем бросает `ReferenceError` при вычислении `n.a`: переменная цикла уже затеняет параметр, но ещё не инициализирована. Новое имя не затеняет параметр.

Ожидаемый результат: исходный код выводит объект и затем `ReferenceError`; после исправления выводятся объект, `1`, `2`, `3`.

Ручная проверка: запустите исходный код и зафиксируйте `ReferenceError`, затем замените имя и сравните три строки.

</details>

<details>
<summary>О задаче</summary>

- Технология: JavaScript
- Подборка: JavaScript → Функции, замыкания и область видимости
- Формат: Разобрать код
- Сложность: Базовая
- Примерное время: 15 минут

</details>
